import styled from "@emotion/styled";

export const AppButton = styled.button({
  borderRadius: 5,
  border: "solid",
  borderWidth: 1,
  paddingTop: 8,
  paddingBottom: 8,
  paddingLeft: 16,
  paddingRight: 16,
});
